# 🚀 DEPLOYMENT GUIDE — NEXX GSM

Пошаговая инструкция для деплоя Next.js приложения (`next-gsm/`) на GitHub Pages або Vercel.

---

## ✅ GitHub Pages (Рекомендовано для статичного сайту)

### Крок 1: Активувати GitHub Pages

1. Відкрийте ваш репозиторій на GitHub: [PlevanDM/SC_BH2026](https://github.com/PlevanDM/SC_BH2026)
2. Перейдіть у **Settings** (⚙️ у верхньому меню)
3. У лівому меню знайдіть розділ **Pages**
4. У секції **Build and deployment**:
   - **Source**: оберіть **GitHub Actions**
   - (НЕ "Deploy from a branch" — це для старого способу!)

### Крок 2: Запустити деплой

Workflow вже налаштований (`.github/workflows/deploy-next-gsm-pages.yml`), тому деплой запуститься автоматично при:
- Будь-якому push в `main` гілку
- Змінах у папці `next-gsm/`

**Або запустіть вручну:**
1. GitHub → репо → **Actions** tab
2. Оберіть workflow **"Deploy next-gsm to GitHub Pages"**
3. Натисніть **Run workflow** → Run workflow

### Крок 3: Перевірити деплой

1. Зайдіть у **Actions** tab — побачите запущений workflow (жовтий кружок = виконується, зелена галочка = успіх)
2. Після успіху сайт буде доступний за адресою:
   ```
   https://plevandm.github.io/SC_BH2026/
   ```
3. Якщо є помилка — клікніть на workflow і подивіться логи

### Крок 4: Налаштувати власний домен (опціонально)

1. Settings → Pages → Custom domain
2. Введіть ваш домен (наприклад `nextgsm.ro`)
3. У DNS вашого домену додайте CNAME запис:
   ```
   CNAME  www  plevandm.github.io
   ```
4. Почекайте 5-15 хвилин для активації SSL

---

## 🔵 Vercel (Рекомендовано для SSR/ISR)

### Переваги Vercel над GitHub Pages:
- ✅ Підтримка Server-Side Rendering (SSR)
- ✅ Incremental Static Regeneration (ISR)
- ✅ Edge Functions та Middleware
- ✅ Автоматичні preview deployments для PR
- ✅ Аналітика та Web Vitals
- ✅ Швидший CDN

### Крок 1: Імпортувати проєкт

1. Зайдіть на [vercel.com](https://vercel.com) та увійдіть через GitHub
2. Натисніть **Add New** → **Project**
3. Оберіть репозиторій **SC_BH2026**
4. Натисніть **Import**

### Крок 2: Налаштувати Build Settings

**ВАЖЛИВО:** Вкажіть правильні налаштування:

```
Root Directory:        next-gsm/          ← ОБОВ'ЯЗКОВО!
Framework Preset:      Next.js
Build Command:         npm run build      (автоматично)
Output Directory:      .next              (автоматично)
Install Command:       npm install        (автоматично)
```

### Крок 3: Environment Variables (якщо потрібно)

Якщо у вас є `.env.local` (наприклад для API ключів):
1. У Vercel → Project Settings → Environment Variables
2. Додайте кожну змінну окремо

### Крок 4: Deploy

1. Натисніть **Deploy**
2. Vercel автоматично зробить білд та деплой
3. Після завершення отримаєте URL типу `https://sc-bh2026-xxx.vercel.app`

### Крок 5: Власний домен (опціонально)

1. Vercel → Project → Settings → Domains
2. Додайте ваш домен (наприклад `nextgsm.ro`)
3. Vercel покаже DNS записи які потрібно додати у вашому реєстраторі
4. SSL автоматично активується

---

## 🆚 GitHub Pages vs Vercel — що обрати?

| Критерій | GitHub Pages | Vercel |
|----------|--------------|---------|
| **Ціна** | ✅ Безкоштовно | ✅ Безкоштовно (Hobby план) |
| **Статичний експорт** | ✅ Так | ✅ Так |
| **SSR/ISR** | ❌ Ні | ✅ Так |
| **Edge Functions** | ❌ Ні | ✅ Так |
| **Preview deployments** | ❌ Ні | ✅ Так (для PR) |
| **Analytics** | ❌ Базовий | ✅ Детальний (Web Vitals) |
| **CDN швидкість** | 🟡 Добре | ✅ Відмінно |
| **Власний домен** | ✅ Так | ✅ Так (з SSL) |
| **Налаштування** | Простіше | Трохи складніше |

**Рекомендація:**
- Якщо сайт **чисто статичний** (без форм/API) → **GitHub Pages**
- Якщо потрібні **динамічні можливості** або **preview deployments** → **Vercel**

---

## 🔧 Тестування локально перед деплоєм

```bash
cd next-gsm

# Dev сервер (швидкий перезавантаження)
npm run dev
# → http://localhost:3000

# Prod білд (статичний експорт)
npm run build
# → перевірте next-gsm/out/

# Перегляньте білд локально
npx serve out -p 3000
# → http://localhost:3000
```

---

## ❓ Troubleshooting

### GitHub Pages: 404 на всіх сторінках крім головної

**Проблема:** Next.js роути не працюють
**Рішення:** Перевірте що у `next.config.ts`:
```ts
output: "export",
trailingSlash: true,
```

### Vercel: Build fails з помилкою "Cannot find module"

**Проблема:** Неправильний Root Directory
**Рішення:** У Vercel Settings → General → Root Directory = `next-gsm/`

### GitHub Actions: Workflow не запускається

**Проблема:** Pages не активовано або Source неправильний
**Рішення:** Settings → Pages → Source = **"GitHub Actions"** (не Branch!)

### Картинки/CSS не завантажуються

**Проблема:** Відносні шляхи ламаються на GitHub Pages
**Рішення:** Переконайтесь що `basePath` в `next.config.ts` правильний (якщо репо не на root domain)

---

## 📞 Потрібна допомога?

- 📖 [Next.js Deployment Docs](https://nextjs.org/docs/app/building-your-application/deploying)
- 🔵 [Vercel Docs](https://vercel.com/docs)
- 🐙 [GitHub Pages Docs](https://docs.github.com/en/pages)

**Готово до прод!** 🎉

