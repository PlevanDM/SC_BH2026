# ⚡ ШВИДКИЙ СТАРТ: GitHub Pages

## 🎯 3 кроки до живого сайту

### Крок 1️⃣: Активувати Pages

1. Відкрийте https://github.com/PlevanDM/SC_BH2026/settings/pages
2. У розділі **"Build and deployment"**:
   - **Source**: оберіть **"GitHub Actions"** ⬅️ ВАЖЛИВО!
   - (НЕ "Deploy from a branch")
3. Збережіть (автоматично)

### Крок 2️⃣: Запустити деплой

**Автоматично:**
Workflow вже налаштований — просто зробіть будь-який push в `main`:
```bash
git commit --allow-empty -m "Trigger deploy"
git push origin main
```

**Або вручну:**
1. https://github.com/PlevanDM/SC_BH2026/actions
2. Оберіть **"Deploy next-gsm to GitHub Pages"**
3. Кнопка **"Run workflow"** → Run workflow

### Крок 3️⃣: Перевірити результат

Після ~2-3 хвилин сайт буде live:
```
https://plevandm.github.io/SC_BH2026/
```

Перевірте статус:
- **Actions tab** (зелена галочка = успіх)
- Якщо помилка — клікніть workflow і подивіться логи

---

## ✅ Готово!

Тепер при кожному push в `main` (з змінами в `next-gsm/`) сайт автоматично деплоїться.

**Детальніша інструкція:** [`DEPLOYMENT.md`](./DEPLOYMENT.md)

---

## 🌐 Власний домен (опціонально)

1. Settings → Pages → **Custom domain**
2. Введіть `nextgsm.ro` (або ваш домен)
3. У DNS вашого домену додайте:
   ```
   CNAME  www  plevandm.github.io
   ```
4. GitHub автоматично видасть SSL сертифікат

Готово! 🚀

