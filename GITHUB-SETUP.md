# 📦 Інструкції для створення репозиторію на GitHub

## ✅ Що вже зроблено:

- ✅ Git репозиторій ініціалізовано
- ✅ Створено `.gitignore` файл (виключено непотрібні файли)
- ✅ Всі потрібні файли додано до git
- ✅ Створено перший коміт

## 🚀 Наступні кроки:

### 1. Створіть репозиторій на GitHub

**Варіант A: Через веб-інтерфейс (найпростіше)**

1. Відкрийте [github.com](https://github.com) і увійдіть в свій акаунт
2. Натисніть **"+"** у правому верхньому куті → **"New repository"**
3. Заповніть форму:
   - **Repository name**: `sc-bucharest` (або ваша назва)
   - **Description**: `SC Bucharest - Сервіс-центр Електроніки`
   - **Visibility**: Public або Private (на ваш вибір)
   - **⚠️ НЕ вмикайте** "Add a README file" (у нас вже є)
   - **⚠️ НЕ додавайте** .gitignore або license (у нас вже є)
4. Натисніть **"Create repository"**

**Варіант B: Через командний рядок (якщо встановлено GitHub CLI)**

```bash
gh repo create sc-bucharest --public --source=. --remote=origin --push
```

### 2. Підключіть локальний репозиторій до GitHub

Після створення репозиторію на GitHub, виконайте одну з команд:

**Якщо репозиторій створено через веб-інтерфейс:**

```bash
git remote add origin https://github.com/YOUR_USERNAME/sc-bucharest.git
git branch -M main
git push -u origin main
```

**Або використайте готовий скрипт:**

```bash
push-to-github.bat https://github.com/YOUR_USERNAME/sc-bucharest.git
```

**Замініть `YOUR_USERNAME` на ваш GitHub username!**

### 3. Автентифікація (якщо потрібно)

Якщо при push запитується пароль:
- GitHub більше не приймає паролі через HTTPS
- Використайте Personal Access Token або SSH ключ

**Варіант A: Personal Access Token**

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token → дайте назву → виберіть `repo` scope
3. Скопіюйте токен і використайте його замість пароля

**Варіант B: SSH ключ (рекомендовано)**

1. Створіть SSH ключ (якщо немає):
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

2. Додайте ключ до GitHub:
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - Скопіюйте вміст `~/.ssh/id_ed25519.pub`

3. Використайте SSH URL:
   ```bash
   git remote set-url origin git@github.com:YOUR_USERNAME/sc-bucharest.git
   git push -u origin main
   ```

## 📋 Файли, які виключені з репозиторію (.gitignore):

- ❌ `cloudflared.exe` - бінарний файл
- ❌ `tunnel-*.txt` - тимчасові файли тунелів
- ❌ `.env*` - конфіденційні дані
- ❌ `node_modules/` - залежності Node.js
- ❌ Системні файли (`.DS_Store`, `Thumbs.db`)

## ✅ Файли, які включені в репозиторій:

- ✅ Весь HTML/CSS/JS код
- ✅ Зображення (`images/`)
- ✅ Docker конфігурації
- ✅ Документація (README, MD файли)
- ✅ Конфігураційні файли (nginx.conf, package.json)
- ✅ Скрипти для запуску (.bat, .sh, .ps1)

## 🎯 Після вивантаження:

Після успішного push, ваш репозиторій буде доступний за адресою:
```
https://github.com/YOUR_USERNAME/sc-bucharest
```

## 🔄 Майбутні оновлення:

Для додавання змін в майбутньому:

```bash
git add .
git commit -m "Опис змін"
git push
```

---

**Питання?** Перевірте [GitHub Docs](https://docs.github.com) або створіть issue в репозиторії.



