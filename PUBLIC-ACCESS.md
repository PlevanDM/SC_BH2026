# 🌐 Публічний Доступ до SC Bucharest

## ✅ Поточний Статус

**Docker контейнер працює на:** `localhost:8080` → `0.0.0.0:8080`

**Локальний IP твого комп'ютера:** `192.168.31.92`

---

## 🔗 Способи Доступу

### 1. Локальний Доступ (тільки на твоєму комп'ютері)

```
http://localhost:8080
http://127.0.0.1:8080
```

### 2. Доступ з Локальної Мережі (WiFi/LAN)

**Відкрий на іншому пристрої в тій же мережі:**

```
http://192.168.31.92:8080
```

**Перевірка:**
- Відкрий на телефоні/планшеті в тій же WiFi мережі
- Або на іншому комп'ютері в локальній мережі

**Якщо не працює:**
- Перевір, чи Windows Firewall не блокує порт 8080
- Перевір, чи пристрій в тій же мережі

### 3. Публічний Доступ (з будь-якого місця в інтернеті)

#### Варіант A: Localtunnel (найпростіший)

**Швидкий запуск:**
```powershell
# Подвійний клік на файл:
start-tunnel.bat

# Або вручну:
npx -y localtunnel --port 8080
```

**Після запуску ти побачиш:**
```
your url is: https://xxxxx.loca.lt
```

**Цей URL можна:**
- Відкрити на будь-якому пристрої
- Поділитися з тестувальниками
- Використовувати для BrowserStack/Live testing

**Зупинити туннель:** `Ctrl+C` в терміналі

#### Варіант B: Ngrok (більш стабільний, але потрібна реєстрація)

1. Зареєструйся на https://ngrok.com (безкоштовно)
2. Завантаж ngrok для Windows
3. Отримай auth token
4. Запусти:

```powershell
ngrok config add-authtoken ТВІЙ_ТОКЕН
ngrok http 8080
```

**Після запуску отримаєш:**
```
Forwarding: https://xxxxx.ngrok.io -> http://localhost:8080
```

#### Варіант C: Cloudflare Tunnel (для продакшену)

Якщо плануєш деплой на сервер, можна використати Cloudflare Tunnel.

---

## 🧪 Тестування з Різних Пристроїв

### На Телефоні (в тій же WiFi)

1. Відкрий `http://192.168.31.92:8080` у браузері телефону
2. Перевір адаптивність, мобільне меню, форми

### Через Туннель (з будь-якого місця)

1. Запусти `start-tunnel.bat`
2. Скопіюй отриманий URL (наприклад, `https://xxxxx.loca.lt`)
3. Відкрий на телефоні/планшеті/іншому комп'ютері
4. Поділися з тестувальниками

### BrowserStack / Cross-Browser Testing

1. Запусти туннель (localtunnel або ngrok)
2. Отримай публічний URL
3. У BrowserStack введи цей URL для тестування
4. Тестуй на різних браузерах/пристроях

---

## 🔧 Налаштування Firewall (якщо локальний IP не працює)

### Windows Firewall

1. Відкрий "Windows Defender Firewall"
2. Натисни "Advanced settings"
3. Створи нове правило:
   - Тип: Inbound Rule
   - Протокол: TCP
   - Порт: 8080
   - Дія: Allow
   - Профіль: Private, Domain

**Або через PowerShell (адміністратор):**
```powershell
New-NetFirewallRule -DisplayName "SC Bucharest Docker" -Direction Inbound -LocalPort 8080 -Protocol TCP -Action Allow
```

---

## 📋 Чек-лист для Тестування

Відкрий файл **`TESTING-CHECKLIST.md`** для детального списку перевірок.

**Швидкий старт:**
1. Відкрий сайт через один з способів вище
2. Пройдися по чек-листу
3. Записуй знайдені баги
4. Перевір на різних пристроях

---

## 🚀 Швидкі Команди

```powershell
# Перевірити статус Docker
docker-compose ps

# Переглянути логи
docker-compose logs -f

# Перезапустити після змін
docker-compose up -d --build

# Зупинити Docker
docker-compose down

# Запустити публічний туннель
.\start-tunnel.bat
```

---

## ⚠️ Важливо

- **Локальний IP (`192.168.31.92:8080`)** працює тільки в локальній мережі
- **Туннель (localtunnel/ngrok)** дає публічний доступ з інтернету
- **Docker контейнер** вже налаштований для зовнішнього доступу (слухає на `0.0.0.0`)
- **Firewall** може блокувати доступ - перевір налаштування

---

## 📞 Підтримка

Якщо щось не працює:
1. Перевір, чи Docker контейнер запущений: `docker ps`
2. Перевір, чи порт 8080 вільний: `netstat -ano | findstr :8080`
3. Перевір логи: `docker-compose logs`
4. Спробуй перезапустити: `docker-compose restart`




